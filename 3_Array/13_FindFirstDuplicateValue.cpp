#include <vector>
#include <unordered_map>
using namespace std;

//Solution with HASH TABLE -> C
//Should perform O(n) worst case, but with ints the hash function should handle it

int firstDuplicateValue(vector<int> array) {
  std::unordered_map<int,int> tracking;
  for (auto & i : array) {

    tracking[i] = trac
  }
  
  return -1; }
